# Сценарии использования

> English version: [docs/en/scenarios.md](../en/scenarios.md).

Зачем нужен lexicon, по задачам: что решает каждый сценарий, какими
вызовами и с каким работающим примером. Справочник по всем упомянутым
вызовам — [library.md](library.md) и [cli.md](cli.md); эта страница
отвечает на вопросы «зачем» и «что выбрать».

У каждого сценария указана версия, с которой он доступен; «X (не
выпущено)» значит, что возможность уже есть в `main`, но ещё не вошла в
версию с тегом (установка: `go get github.com/amarin/lexicon@main`). Ещё три пометки
ведут мини-changelog прямо рядом с возможностью:

- **Поведение в 0.1.0** (**Поведение в 0.2**, …) — правило, принятое
  осознанным решением и способное измениться. Когда оно изменится, строка
  переедет в **Историю**.
- **История** — что поменялось в следующих версиях; выдержка из
  [CHANGELOG.md](../../CHANGELOG.md) по конкретной возможности (источник
  истины — CHANGELOG).
- **⚠ переиндексация** — изменение меняет формы или термы: поднимается
  `textnorm.Rules.Version` или версия анализатора, меняется
  `Analyzer.Version()`, и хосту нужно пересобрать производные данные (см.
  [сценарий 13](#13-понять-когда-переиндексировать)). Такое изменение меняет
  и спаны.
- **⚠ переизвлечение** — изменение меняет извлечённые спаны, но не термы
  индекса: поднимается версия экстрактора, меняется `ner.Result.Version`,
  и хост пересчитывает сохранённые спаны или предложения разметки, но не
  поисковый индекс.

Примеры: `go run ./examples/<name>` из корня репозитория
([examples/](../../examples/README.md)); функции `ExampleXxx` — в
`example_test.go` и `textnorm/example_test.go`, они отображаются на
pkg.go.dev. Все примеры, кроме `base`, работают без скачивания: словари,
газетиры и правила в них — крошечные TSV и строки YAML, заданные в коде.

| # | Сценарий | С версии | Пример |
|---|---|---|---|
| 1 | [Сравнивать слова независимо от орфографии](#1-сравнивать-слова-независимо-от-орфографии) | 0.1.0 | [orthography](../../examples/orthography/main.go), `ExampleNormalizeWord` |
| 2 | [Исправить латиницу внутри кириллических слов](#2-исправить-латиницу-внутри-кириллических-слов) | 0.1.0 | [orthography](../../examples/orthography/main.go) |
| 3 | [Токены со смещениями в исходный текст](#3-токены-со-смещениями-в-исходный-текст) | 0.1.0 | [tokenize](../../examples/tokenize/main.go), `ExampleTokenize`, `ExampleUTF16Offsets` |
| 4 | [Термы для поискового индекса](#4-термы-для-поискового-индекса) | 0.1.0 | [index](../../examples/index/main.go), `ExampleAnalyzer_Analyze` |
| 5 | [Разбор поискового запроса по мере ввода](#5-разбор-поискового-запроса-по-мере-ввода) | 0.1.0 | [query](../../examples/query/main.go), `ExampleAnalyzer_ParseQuery` |
| 6 | [Профиль на поле против омонимии](#6-профиль-на-поле-против-омонимии) | 0.1.0 | [profiles](../../examples/profiles/main.go), `ExampleProfile` |
| 7 | [Разметить каждый токен (вход для NER и проверки)](#7-разметить-каждый-токен-вход-для-ner-и-проверки) | 0.1.0 | [full](../../examples/full/main.go), `ExampleAnalyzer_Analyze_full` |
| 8 | [Дореформенные тексты](#8-дореформенные-тексты) | 0.1.0 | [full](../../examples/full/main.go), `ExampleReformVariants` |
| 9 | [Сокращения в записях](#9-сокращения-в-записях) | 0.1.0 | [abbrev](../../examples/abbrev/main.go) |
| 10 | [Свои словари: файлы и встроенные](#10-свои-словари-файлы-и-встроенные) | 0.1.0 | [registry](../../examples/registry/main.go), [embed](../../examples/embed/main.go), `ExampleOpen` |
| 11 | [Базовый словарь и его атрибуция](#11-базовый-словарь-и-его-атрибуция) | 0.1.0 | [base](../../examples/base/main.go), `ExampleParseManifest` |
| 12 | [Включать и выключать словари, перезагрузка без рестарта](#12-включать-и-выключать-словари-перезагрузка-без-рестарта) | 0.1.0 | [registry](../../examples/registry/main.go) |
| 13 | [Понять, когда переиндексировать](#13-понять-когда-переиндексировать) | 0.1.0 | [registry](../../examples/registry/main.go), `ExampleAnalyzer_Version` |
| 14 | [Проверить руками](#14-проверить-руками) | 0.1.0 | CLI `analyze`, `dicts list` |
| 15 | [Найти сущности по словарям](#15-найти-сущности-по-словарям) | 0.2.0 | [ner](../../examples/ner/main.go) |
| 16 | [Слова контекста, триггеры и теги документа](#16-слова-контекста-триггеры-и-теги-документа) | 0.2.0 | [ner](../../examples/ner/main.go) |
| 17 | [Пересекающиеся совпадения, вложенность и объяснения](#17-пересекающиеся-совпадения-вложенность-и-объяснения) | 0.2.0 | [ner](../../examples/ner/main.go) |
| 18 | [Обновлять газетиры без рестарта](#18-обновлять-газетиры-без-рестарта) | 0.2.0 | [gazetteer](../../examples/gazetteer/main.go) |
| 19 | [Измерить качество на эталонном наборе](#19-измерить-качество-на-эталонном-наборе) | 0.2.0 | [golden](../../examples/golden/main.go) |
| 20 | [Попробовать NER руками](#20-попробовать-ner-руками) | 0.2.0 | CLI `extract`, `golden` |
| — | [Запланировано: паттерны](#запланировано-паттерны) | 0.3 | — |

## 1. Сравнивать слова независимо от орфографии

**Задача.** Чтобы «Вѣра», «Вера» и «ВЕРА», «Ѳеодоръ» и «Федор», «ёлка» и
«елка» совпадали — в поисковом индексе, при дедупликации, при сопоставлении
запроса со старыми документами.

**Как.** Выбрать набор правил: `textnorm.Modern` (нижний регистр, NFC,
ё→е, диакритика удаляется, кроме й) или `textnorm.PreReform` (Modern плюс
ѣ→е, і→и, ѳ→ф, ѵ→и и другие старые буквы, и конечный ъ, удаляемый в каждой
части слова через дефис). `textnorm.NormalizeWord(r, w)` — форма, по
которой слово индексируется и сравнивается; `textnorm.Orthography(r, s)`
приводит целую строку (конечный ъ — правило слова, он остаётся). Результат
годится только для сравнения; смещения на него никогда не указывают
([сценарий 3](#3-токены-со-смещениями-в-исходный-текст)).

```go
textnorm.NormalizeWord(textnorm.PreReform, "Санктъ-Петербургъ") // санкт-петербург
textnorm.NormalizeWord(textnorm.Modern, "Ёлка")                 // елка
```

Свой набор правил — это новое значение `textnorm.Rules` со своими `Name` и
`Version`; `Modern`/`PreReform` менять нельзя.

**Пример:** [orthography](../../examples/orthography/main.go),
`ExampleNormalizeWord`, `ExampleOrthography`; CLI `analyze --ortho
prereform`.

**Доступно с:** 0.1.0 (`Modern` и `PreReform` с `Version` "3").

## 2. Исправить латиницу внутри кириллических слов

**Задача.** В выходе OCR и в набранном вручную тексте в кириллические
слова попадают похожие латинские буквы: «Kот», «Iоаннъ». Такое слово
никогда не совпадёт со своей словарной формой.

**Как.** Вызывать ничего не нужно: это делает `Rules.Homoglyphs` в
токенизаторе и в `Orthography`. Часть слова (части разделяются дефисами),
в которой есть кириллическая буква и нет других букв, кроме известных
гомоглифов, считается кириллической: токенизатор оставляет её одним словом
`ScriptCyrillic`, а гомоглифы заменяются. Части с другими латинскими
буквами («Kотw») или без кириллицы («XIX») не трогаются. `PreReform` также
считает латинские i/I дореформенными і/І.

**Пример:** [orthography](../../examples/orthography/main.go) («Kот»,
«Iоаннъ», «XIX»).

**Доступно с:** 0.1.0. Буквы с диакритикой (латинская буква с ударением
внутри кириллического слова) тоже приводятся.

## 3. Токены со смещениями в исходный текст

**Задача.** Подсветить найденное слово, подчеркнуть сущность или вырезать
цитату из текста ровно в том виде, как он хранится, — хотя сопоставление
шло по нормализованной форме, и даже если клиент на JavaScript (строки
UTF-16).

**Как.** `textnorm.Tokenize(r, input)` возвращает все токены — слова любой
письменности, числа, пунктуацию, символы — с байтовыми смещениями
(`Start`/`End`, всегда `input[Start:End] == Raw`) и смещениями в кодовых
точках (`RuneStart`/`RuneEnd`); границы проходят по графемным кластерам.
`textnorm.UTF16Offsets(input, offs...)` переводит байтовые смещения для
JavaScript. `textnorm.SplitHyphen(tok)` даёт части слова через дефис, у
каждой свои смещения и `Form`. `Token.Dotted` отмечает слово, за которым
сразу идёт «.» (кандидат в сокращения); `SentenceEnd` — «.», «!», «?», «;»,
«…» и последний токен перед пустой строкой.

```go
for _, t := range textnorm.Tokenize(textnorm.PreReform, input) {
    fmt.Println(input[t.Start:t.End], t.Form, t.RuneStart, t.RuneEnd)
}
```

**Пример:** [tokenize](../../examples/tokenize/main.go),
`ExampleTokenize`, `ExampleSplitHyphen`, `ExampleUTF16Offsets`.

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:** смещения всегда указывают на вход ровно в том виде,
в каком он передан в `Tokenize`, и никогда — на нормализованную строку. Это
обязательный контракт, а не настройка по умолчанию: он не изменится.

## 4. Термы для поискового индекса

**Задача.** Превратить поле документа в термы индекса: «Кота», «котом» →
«кот»; служебные слова отброшены; числа сохранены; неоднозначная форма
(«стали» → «сталь», «стать») индексируется под каждой леммой.

**Как.** Открыть `Registry` ([сценарий 10](#10-свои-словари-файлы-и-встроенные)),
создать один `Analyzer` через `lexicon.NewAnalyzer(reg, rules,
AnalyzerOptions{})` и вызвать `Analyze(text, profile, lexicon.ModeIndex)`.
У каждого `Term` есть исходный `Token`, форма поиска `Form` и `Lemmas`
(`Text`, `Flags`, а также `Tag`, `Kind`, `Dict` разбора, давшего лемму).
Индексируйте леммы; `Form` тоже сохраните, если ищете по префиксу
([сценарий 5](#5-разбор-поискового-запроса-по-мере-ввода)). Слово через
дефис индексируется целиком и по частям. Индексируются только кириллические
слова и числа.

Флаги говорят, насколько лемме можно доверять: без флагов — словарное
слово; `ambiguous` — одна из нескольких лемм; `predicted` — слова нет ни в
одном словаре, лемму угадал базовый словарь по окончанию; `unknown` —
разборов нет вовсе, лемма — сама форма; `abbrev`, `reform` — см. сценарии
[9](#9-сокращения-в-записях) и [8](#8-дореформенные-тексты).

```go
a := lexicon.NewAnalyzer(reg, textnorm.Modern, lexicon.AnalyzerOptions{})
for _, t := range a.Analyze("Кота и пса стали кормить", lexicon.Profile{Name: "text"}, lexicon.ModeIndex) {
    for _, l := range t.Lemmas {
        index.Add(docID, l.Text)
    }
}
```

`Analyzer` безопасен для конкурентного использования и кэширует леммы по
имени профиля (`AnalyzerOptions.CacheSize`, по умолчанию 50 000 записей;
кэш сбрасывается при изменении набора словарей). Используйте один
`Analyzer` и для индексации, и для разбора запросов — тогда они не
разойдутся.

**Пример:** [index](../../examples/index/main.go),
`ExampleAnalyzer_Analyze`; с настоящим базовым словарём —
[base](../../examples/base/main.go).

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:**
- Стоп-слово — слово, все точные разборы которого — служебные части речи
  (PREP, CONJ, PRCL, INTJ); разборы с `Abbr` не учитываются (буквенные
  «сокращения» OpenCorpora для «в», «с», «и»). Списка стоп-слов нет: у
  «что», «как», «из» есть неслужебные разборы, и они индексируются.
- Предсказания используются только если ни у формы, ни у её дореформенных
  вариантов нет точного разбора, и только из словарей вида `base`. Если
  профиль отфильтровал все точные разборы, лемма — `unknown`: догадка
  никогда не подменяет отфильтрованное словарное слово.

## 5. Разбор поискового запроса по мере ввода

**Задача.** Строка поиска: «Кузнецова Ив» должна находить документы с
«Кузнецов» в любой форме и с любым словом, начинающимся на «ив».

**Как.** `Analyzer.ParseQuery(q, profile)` разбирает запрос так же, как
`ModeIndex`. Последнее слово, если после него ничего нет (даже пробела),
становится `Query.Partial`: ищите его по префиксу среди проиндексированных
форм и по его леммам (объединение). Служебное слово в конце без лемм и
ищется только по префиксу. `Query.Terms` — законченные слова. Число или
слово с точкой в конце считаются законченными.

```go
q := a.ParseQuery("кота Кузн", name)
// q.Terms: кота → кот; q.Partial: кузн — префикс «кузн*» или его леммы
```

**Пример:** [query](../../examples/query/main.go),
`ExampleAnalyzer_ParseQuery`.

**Доступно с:** 0.1.0.

## 6. Профиль на поле против омонимии

**Задача.** В поле «имя» «Вера» — имя, а не «вера», «Мороз» — фамилия, а
не «мороз»; в поле «место» «Покровском» — село, а не прилагательное. Всё
это на одном наборе словарей.

**Как.** `Profile` — конфигурация хоста: `Name` (ключ кэша, уникален в
пределах `Analyzer`), `Kinds` (виды словарей, по которым ищем; пусто — все)
и `Grammemes` (для вида: разбор остаётся, только если в его теге есть одна
из граммем). Виды хоста (`surname`, `given`, `toponym`, …) объявляются в
`Options.Kinds`. Разборы идут в порядке реестра, лемма хранит тег первого
давшего её разбора.

```go
name := lexicon.Profile{
    Name:      "name",
    Kinds:     []lexicon.Kind{lexicon.KindBase, "surname", "given", "patronymic"},
    Grammemes: map[lexicon.Kind][]string{lexicon.KindBase: {"Name", "Surn", "Patr"}},
}
place := lexicon.Profile{
    Name:      "place",
    Kinds:     []lexicon.Kind{lexicon.KindBase, "toponym", lexicon.KindAbbrev},
    Grammemes: map[lexicon.Kind][]string{lexicon.KindBase: {"Geox"}},
}
```

CLI: `--profile 'name:base[Name|Surn|Patr],surname'`.

**Пример:** [profiles](../../examples/profiles/main.go), `ExampleProfile`.

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:** профили не входят в `Analyzer.Version()`. Хост,
изменивший определение профиля, версионирует его сам и переиндексирует
поля, которые его используют.

## 7. Разметить каждый токен (вход для NER и проверки)

**Задача.** Показать текст с разметкой каждого слова — для предварительной
разметки перед проверкой человеком, для интерфейса с подсветкой или для
своих правил поиска сущностей, — не теряя пунктуацию и служебные слова,
важные для контекста.

**Как.** `Analyze(text, profile, lexicon.ModeFull)` возвращает ровно один
`Term` на токен, включая пунктуацию (у пунктуации и символов лемм нет).
Служебные слова сохраняют леммы с флагом `stop`, а не отбрасываются. Числа
и некириллические слова получают лемму `unknown` без обращения к словарю.
Смещения берутся из токена
([сценарий 3](#3-токены-со-смещениями-в-исходный-текст)).

**Пример:** [full](../../examples/full/main.go),
`ExampleAnalyzer_Analyze_full`; CLI `analyze --mode full`.

**Доступно с:** 0.1.0.

**История:** 0.2.0 — словарный NER работает поверх этого
режима: газетиры, правила и разрешённые спаны, сценарии
[15](#15-найти-сущности-по-словарям)–[20](#20-попробовать-ner-руками).

## 8. Дореформенные тексты

**Задача.** Разбирать документы до 1918 года: «Иванъ Кузнецовъ изъ села
Покровскаго» — старые буквы, конечный ъ и окончания прилагательных -аго,
-яго, -ыя, -ія, которых нет ни в одном современном словаре.

**Как.** Создать `Analyzer` с `textnorm.PreReform` (буквы и ъ,
[сценарий 1](#1-сравнивать-слова-независимо-от-орфографии)). Для окончаний
ничего не нужно: если у формы нет точного разбора, анализатор по порядку
пробует её современные варианты из `textnorm.ReformVariants` и берёт первый,
известный словарям; лемма получает флаг `reform`, а индексируемая `Form`
остаётся как в тексте («покровскаго»).

```go
textnorm.ReformVariants("покровскаго") // [покровского]
textnorm.ReformVariants("хорошаго")    // [хорошого хорошего]: окончание зависит от ударения
```

**Пример:** [full](../../examples/full/main.go) («Шуйскаго» →
«шуйский[reform]»), `ExampleReformVariants`; CLI `analyze --ortho
prereform`.

**Доступно с:** 0.1.0.

## 9. Сокращения в записях

**Задача.** Метрические книги и ревизские сказки полны сокращений:
«кр-нин» (крестьянин), «с.» (село или сын), «у.» (уезд), «губ.»
(губерния). Индексировать и размечать их по полным словам.

**Как.** Подключить словарь вида `abbrev` — файл `abbrev.<name>.tsv` или
встроенный, — у которого в колонке словоформы стоит сокращение с точкой
или дефисом, а в колонке леммы — полное слово. Слово с точкой сначала
ищется вместе с точкой, слово через дефис — сначала целиком; лемма
получает флаг `abbrev`, а `Term.Form` включает точку («с.»). Список
сокращений lexicon не поставляет: это данные хоста.

```
крестьянин	кр-нин	NOUN
село	с.	NOUN
сын	с.	NOUN
```

**Пример:** [abbrev](../../examples/abbrev/main.go).

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:**
- Однобуквенное сокращение с точкой («с.», «ц.») всегда `ambiguous`;
  однобуквенное слово без точки («с» — предлог) никогда не получает
  разбора-сокращения.
- Поиск сокращений не учитывает `Kinds` профиля: профиль без `abbrev` всё
  равно раскрывает «с.». Строгая фильтрация по профилю — открытый вопрос
  ([todo](../todo.md)).
- Без словаря сокращений решает базовый словарь: в базе OpenCorpora «г.»
  даёт «г», «кр-нин» угадывается.

## 10. Свои словари: файлы и встроенные

**Задача.** Добавить словарь хоста — фамилии, имена, топонимы, сословия —
в виде словарей, которые пользователи могут править, а часть поставлять
внутри бинарника.

**Как.** `lexicon.Open(ctx, lexicon.Options{...})`:
- `Dir` — каталог с файлами `<kind>.<name>.dat` (бинарный формат gomorphy,
  отображается в память) и `<kind>.<name>.tsv`
  (`lemma<TAB>wordform[<TAB>tags]`, собирается при загрузке через
  `ImportTSV` из gomorphy). Имя словаря — имя файла без расширения.
  Символические ссылки разыменовываются.
- `<file>.meta` — необязательный файл происхождения из строк
  `key: value` (`source`, `license`, `version`, `url`, `generated_from`),
  доступен как `Entry.Manifest`.
- `Builtin` — словари хоста в памяти (`//go:embed`), `FormatTSV` или
  `FormatDat`. Файл с тем же именем в `Dir` заменяет встроенный словарь,
  так что пользователи могут переопределить поставляемые данные без
  пересборки.
- `Kinds` — собственные виды хоста. Файл необъявленного вида (опечатка
  вроде `surnme.x.tsv`) виден в списке с ошибкой и не используется; nil
  принимает любой допустимый вид (так делает CLI).

Порядок реестра: базовый словарь, затем встроенные по порядку, затем
остальные файлы по имени. Испорченный файл никогда не ломает `Open`:
`Registry.List()` показывает его с `Entry.Error`, `Registry.Summary()`
учитывает его.

**Пример:** [registry](../../examples/registry/main.go),
[embed](../../examples/embed/main.go), `ExampleOpen`; CLI `dicts list`.

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:** предсказания дают только словари вида `base`.
gomorphy строит предсказание для любого собранного словаря, и маленький
список фамилий иначе «узнавал» бы почти любое слово.

## 11. Базовый словарь и его атрибуция

**Задача.** Получить общую морфологию русского языка (OpenCorpora, ~15 МБ),
не храня её в репозитории, и правильно указать атрибуцию (CC BY-SA 4.0) при
распространении.

**Как.** Три способа:
- CLI: `lexicon dicts fetch [--dicts DIR]` скачивает pymorphy2-dicts-ru,
  компилирует и записывает `base.opencorpora.dat` вместе с файлом `.meta`.
- Go: `basefetch.Fetch(dst)`. Только этот пакет тянет загрузчик pymorphy из
  gomorphy и логгер (zap); хосты, которые его не импортируют, их не
  компилируют.
- Встроить: положить байты `.dat` в `Options.Base`, а `.meta`, записанный
  `basefetch`, разобрать через `lexicon.ParseManifest` и передать в
  `Options.BaseManifest`. Словарь регистрируется как `base.builtin`; любой
  файл `base.*` в `Dir` его заменяет.

`Entry.Manifest` базового словаря содержит источник, версию, URL и
лицензию: показывайте их везде, где указываете атрибуцию данных. Сам
lexicon словарных данных не поставляет.

**Пример:** [base](../../examples/base/main.go),
`ExampleParseManifest`; CLI `dicts fetch`, `dicts list`.

**Доступно с:** 0.1.0.

## 12. Включать и выключать словари, перезагрузка без рестарта

**Задача.** Дать администратору отключить «шумный» словарь или подложить
новый список фамилий, пока сервис продолжает отвечать на запросы.

**Как.** `Registry.SetEnabled(ctx, name, on)` сохраняет состояние через
`Options.State` (`StateStore`, который хост реализует поверх своей БД; nil
— в памяти, всё включено) и подменяет снимок. `Registry.Reload(ctx)`
заново сканирует `Dir`, переоткрывает все словари и подменяет снимок.
Читатели никогда не блокируются: `Parse` фиксирует текущий снимок, а
старые словари закрываются, когда завершатся начатые вызовы.

Заменяйте файлы словарей атомарно — запишите временный файл и
переименуйте его поверх старого. `.dat` отображается в память:
перезапись на месте может обрушить идущие разборы.

**Пример:** [registry](../../examples/registry/main.go).

**Доступно с:** 0.1.0.

**Поведение в 0.1.0:** `Reload` переоткрывает все словари, даже если файл
не менялся (переиспользование по хэшу — открытый вопрос,
[todo](../todo.md)).

## 13. Понять, когда переиндексировать

**Задача.** Поисковый индекс хранит леммы, вычисленные какой-то версией
правил на каком-то наборе словарей. Нужно знать, когда они устарели.

**Как.** Храните `Analyzer.Version()` вместе с производными данными и
пересобирайте их при его изменении. Значение складывается из трёх частей:

| Часть | Меняется, когда | Пример значения |
|---|---|---|
| версия анализатора | правила анализа в библиотеке меняют термы | `analyzer-2` |
| `Rules.Name`-`Rules.Version` | таблица или правило орфографии меняет формы | `prereform-3` |
| `Registry.Version()` | словарь добавлен, удалён, включён, выключен или изменился его состав | хэш содержимого |

У извлечённых спанов своя версия, `ner.Result.Version`
([сценарий 15](#15-найти-сущности-по-словарям)). Она меняется вместе с
`Analyzer.Version()`, а также когда меняется:

| Часть | Меняется, когда | Пример значения |
|---|---|---|
| версия экстрактора | правила NER в библиотеке меняют спаны (⚠ переизвлечение) | `ner-1` |
| снимок газетира | источник перекомпилирован из нового содержимого или новым анализатором | хэш |
| книга правил | меняются файлы правил | хэш |
| конфигурация конвейера | меняются профили, веса, вложенность, `MinLemmaMatchRunes` | хэш |

Профили — определения хоста и в неё не входят
([сценарий 6](#6-профиль-на-поле-против-омонимии)). Для сброса кэшей
словарных запросов достаточно одного `Registry.Version()`.

**Пример:** [registry](../../examples/registry/main.go),
`ExampleAnalyzer_Version`; CLI `analyze` печатает версию в stderr.

**Доступно с:** 0.1.0 (версия анализатора "2", `Modern` и `PreReform` —
"3").

**История:** каждая будущая запись с пометкой ⚠ переиндексация или
⚠ переизвлечение на этой странице называет часть, которую она поднимает.

## 14. Проверить руками

**Задача.** Посмотреть, что lexicon делает с фразой, какие словари
загружены и откуда они, — без написания кода.

**Как.** `lexicon analyze [--dicts DIR] [--ortho modern|prereform]
[--profile SPEC] [--mode index|full] TEXT...` печатает по строке на терм:
исходный текст, форму, леммы с флагами; сводка по словарям и
`Analyzer.Version()` идут в stderr. `lexicon dicts list` печатает каждый
словарь с видом, форматом, состоянием, источником, хэшем, происхождением и
ошибкой. См. [cli.md](cli.md).

**Доступно с:** 0.1.0.

## 15. Найти сущности по словарям

**Задача.** Разметить в записи людей, места и сословия: «Иван Петров из
деревни Лягушкино Боровского уезда» → имя, фамилия и два места, и каждое
ссылается на собственную запись хоста — с учётом словоизменения
(«Боровского» → «Боровский») и вариантов написания («Иоанн» → «Иван»).

**Как.** Три части.
- **Газетир.** Псевдонимы записей хоста берутся из `gazetteer.Source`:
  файл TSV (`gazetteer.NewTSVSource(name, path)`), байты TSV, встроенные
  хостом (`NewTSVSourceData`), записи, собранные в Go (`NewSliceSource`),
  или собственная реализация хоста поверх его базы данных. Строка TSV —
  `type<TAB>ref<TAB>canonical<TAB>alias[<TAB>flags[<TAB>k=v;k=v]]`; строки
  `# key: value` до первой записи — манифест происхождения. Псевдонимы с
  общим `Ref` — варианты одной записи. `gazetteer.New` разбирает каждый
  псевдоним вашим `Analyzer` (по типам — через `TypeProfiles`) и
  компилирует для него ключ по поверхностной форме и ключи по леммам (по
  одному на сочетание лемм его слов, не больше `gazetteer.MaxLemmaKeys` =
  8; у `SurfaceOnly` их нет).
- **Правила** (необязательно, [сценарий 16](#16-слова-контекста-триггеры-и-теги-документа)).
- **Конвейер.** `ner.New(ner.Config{Analyzer, Gazetteer, Rules, Profiles,
  DefaultProfile})`, затем `Extract(ctx, ner.Doc{Text, Profile, Tags,
  Types})`.

```go
gz, _ := gazetteer.New(ctx, gazetteer.Config{
    Analyzer: an, DefaultProfile: text,
    Sources:  []gazetteer.Source{gazetteer.NewTSVSource("places", "place.tsv")},
})
p, _ := ner.New(ner.Config{Analyzer: an, Gazetteer: gz, Rules: book,
    Profiles: map[string]lexicon.Profile{"text": text}, DefaultProfile: "text"})
res, _ := p.Extract(ctx, ner.Doc{Text: "из деревни Лягушкино Боровского уезда"})
for _, s := range res.Spans {
    fmt.Println(s.Surface, s.Type, s.Refs, s.Normal) // Боровского уезда division [d2] [Боровский]
}
```

У `Span` есть байтовые смещения и смещения в кодовых точках в `Doc.Text`
(`Doc.Text[Start:End] == Surface`), тип `Type`, ссылки хоста `Refs`
(непрозрачные ключи; пусто у кандидата от триггера), `Normal` (формы
`Canonical` совпавших записей; у кандидата от триггера — леммы его слов),
атрибуты записей `Attrs`, оценка `Score` и флаги `Flags`: `Ambiguous`
(несколько ссылок или нормальных форм, покрытое неоднозначное сокращение
вроде «с.», которое не поглотило правило, или ничья между типами — но не
омонимия обычного слова), `Predicted`, `Abbrev`, `Candidate`
(предложен триггером, а не записью) и `Nested`. Флаги записи настраивают
сопоставление: `SurfaceOnly` (без ключа по леммам — для сокращений вроде
«СПб»), `CaseSensitive`, `RequiresContext` (остаётся, только если его
поддержало правило: «Мороз» — фамилия, а не «мороз») и `Blocked` (эти слова
— не этот тип). `Result.Version` покрывает экстрактор, анализатор,
скомпилированный газетир, правила и конфигурацию: храните её вместе с
предложениями разметки и пересчитывайте при её изменении.

lexicon только размечает: связать спан с конкретным человеком или местом —
работа хоста.

**Пример:** [ner](../../examples/ner/main.go); CLI `extract`
([сценарий 20](#20-попробовать-ner-руками)).

**Доступно с:** 0.2.0.

**Поведение в 0.2:**
- Однословное совпадение, найденное только по лемме, отбрасывается, если
  псевдоним короче `Config.MinLemmaMatchRunes` (по умолчанию 3 кодовые
  точки),
  поэтому «с» никогда не совпадёт по лемме с однобуквенным псевдонимом.
- `Blocked` запрещает независимо от остальных флагов записи:
  заблокированная запись с `CaseSensitive` запрещает при любом регистре, а
  заблокированное однословное совпадение по лемме запрещает даже ниже
  `MinLemmaMatchRunes`.
- Псевдоним совпадает только с подряд идущими словами без пунктуации между
  ними («Большой, Лес» не совпадает с «Большой Лес»); собственная точка
  сокращения пропускается.
- Спан, который кончается сокращением с точкой, точку не включает
  («Калужской губ», а не «Калужской губ.»); расширить спан на точку
  планируется в 0.3.
- Группы вариантов (псевдонимы с общим `Ref`) при извлечении не
  используются: `Normal` спана берётся из полей `Canonical` совпавших
  записей ([сценарий 18](#18-обновлять-газетиры-без-рестарта)).
- Интернер скомпилированных псевдонимов общий на процесс и никогда не
  уменьшается.

## 16. Слова контекста, триггеры и теги документа

**Задача.** Использовать слова вокруг имени: «деревни», «уезда», «ул.»
говорят, что рядом место; «село Покровское» — место, даже если записи о нём
нет; в дореволюционных записях о крестьянах слово после «крестьянин» —
скорее всего фамилия, а в других документах — нет.

**Как.** Файл правил — YAML (или JSON) с необязательным отображением
происхождения `meta:` на верхнем уровне и наборами правил `sets:`;
загрузите его `rules.LoadFile`, `LoadNamed` или `Load` и скомпилируйте один
или несколько файлов `rules.Compile` в `Book` для `ner.Config.Rules`.
- **Подсказка** (hint) — ключевое слово (`lemma: деревня|село`,
  `dotted: true` для «ул.»), которое прибавляет свой `weight` к спанам
  своего `type`, чья граница не дальше `window` значимых слов в
  направлении `dir` (`right`, `left`, `both`; `window: 1` — вплотную к
  ключевому слову), удовлетворяет их `RequiresContext`, а с `absorb: true`
  расширяет соседний спан на само ключевое слово. У подсказок нет `shape`.
- **Триггер** предлагает спан-кандидат (`Candidate`) своего `type`,
  покрывающий слова после (или до) ключевого слова — `window: 1..2` слов,
  подходящих под `shape` (`case`, `script`), до первого `stop_at`
  (`punct` всегда, `stop`, `number`, `latin`), — если ни один спан
  газетира этого типа их не перекрывает; иначе он усиливает перекрывающие
  спаны. Его `weight` прибавляется в обоих случаях — к предложенному
  кандидату или к усиленным спанам, а `absorb: true` расширяет и тех, и
  других на соседнее ключевое слово. Триггер с `negative: true` вычитает
  свой вес из перекрывающих спанов газетира своего типа и никогда ничего не
  предлагает и не поглощает.
- **Набор правил** с `when: [теги]` активен, только если `Doc.Tags`
  документа содержит ВСЕ эти теги; набор без `when` активен всегда.

```yaml
meta: {source: my rules, license: CC0-1.0}
sets:
  - name: places
    hints:
      - {lemma: уезд, type: division, dir: left, window: 1, weight: 2, absorb: true}
    triggers:
      - {lemma: деревня|село, type: division, window: 1..2, shape: {case: title, script: cyrillic}, absorb: true}
  - name: pre1917
    when: ["period:pre1917"]
    hints:
      - {lemma: крестьянин, type: surname, window: 2}
```

Ключевые слова — леммы или формы, они сравниваются после дореформенной
нормализации, так что один файл правил подходит и к современному, и к
дореформенному тексту. Окна считают значимые слова от ключевого слова до
спана и не переходят через пунктуацию и конец предложения (точка
сокращения их не прерывает); `rules.MaxWindow` = 8. Ошибки указывают
место: `file.yaml:12: places/hint 0: …`.

**Пример:** [ner](../../examples/ner/main.go) (подсказка, кандидат от
триггера, набор, включаемый тегом `period:pre1917`); CLI `extract --rules
--tags`.

**Доступно с:** 0.2.0.

**Поведение в 0.2:**
- Отрицательный триггер действует только на спаны газетира, но не на
  кандидатов от триггеров. Спан с оценкой 0 или ниже удаляется, так что
  отрицательный триггер (или отрицательные веса) может убрать совпадение
  целиком.
- Подсказки выполняются до триггеров и никогда не усиливают кандидатов от
  триггеров.
- Запись с `Blocked` останавливает и триггеры: кандидат заблокированного
  типа не предлагается на диапазоне, пересекающем заблокированное
  совпадение.
- Подсказка отсчитывает окно от текущей границы спана; если одна подсказка
  уже поглотила своё ключевое слово, следующая отсчитывает от новой
  границы, поэтому результат может зависеть от порядка подсказок.
- Окно триггера может захватить следующее за ним имя другого типа раньше,
  чем будет рассмотрен собственный кандидат этого имени; держите окна
  триггеров короткими.
- Правила компилируются один раз: чтобы их изменить, соберите новый
  `Pipeline` (горячая замена — открытый вопрос, [todo](../todo.md)).

## 17. Пересекающиеся совпадения, вложенность и объяснения

**Задача.** «Боровского уезда» совпадает сразу с псевдонимом места, с
триггером и, может быть, с фамилией; название улицы может лежать внутри
спана города. Нужно выбрать один ответ на каждый участок текста, сохранить
проигравшие варианты и показать проверяющему, почему спан найден.

**Как.** `Extract` разрешает кандидатов взвешенным планированием
интервалов: никакие два спана результата не пересекаются, а вложить спан в
другой можно только для пары внешнего и внутреннего типов из
`Config.Nesting` (внутренний получает флаг `Nested`). Оценка кандидата —
взвешенная сумма (`ner.Weights`, `DefaultWeights()`, если веса нулевые):
на каждое слово — вес происхождения (поверхность 3, лемма 2, триггер 1)
плюс вес типа, бонус за длину на каждое слово после первого (0,5),
вклад подсказок и триггеров, минус штраф за неоднозначность (0,25),
умноженный на (большее из чисел различных ссылок и нормальных форм − 1).
Кандидаты с оценкой 0 или ниже отбрасываются до разрешения. Кандидаты других типов на том
же самом диапазоне, что и победитель, становятся `Span.Alternatives`
(лучшие первыми); точная ничья никогда не бывает молчаливой — спан
получает `Ambiguous`, а ничья разрешается по имени типа. `Doc.Types`
скрывает ненужные типы уже после разрешения, поэтому никогда не меняет
победителя. `ner.Explain()` заполняет `Span.Evidence`: «lemma match
«Боровский» (places)», «hint «уезда» → division +2».

```go
p, _ := ner.New(ner.Config{..., Nesting: map[string][]string{"city": {"street"}}})
res, _ := p.Extract(ctx, doc, ner.Explain())
```

**Пример:** [ner](../../examples/ner/main.go) (обоснование каждого
спана); CLI `extract --nest 'city>street' --explain` (`>` нужно взять в кавычки
от shell).

**Доступно с:** 0.2.0.

## 18. Обновлять газетиры без рестарта

**Задача.** Пользователь добавил в приложении хоста деревню или вариант
фамилии; извлечение должно увидеть это за секунды, а остальные запросы —
продолжать работать.

**Как.** `Source.Version(ctx)` должен быть дешёвым и меняться вместе с
содержимым (`TSVSource` хэширует свои байты; источник хоста может
возвращать счётчик ревизий). `Gazetteer.Refresh(ctx)` перекомпилирует
только источники, у которых изменилась версия (или версия анализатора),
`RefreshSource(ctx, name)` — один источник независимо от версии; оба
возвращают `SourceReport` (записи, псевдонимы, ключи, урезанные
раскрытия, заблокированные записи, длительность, плохие строки). Новый
`Snapshot` подменяется атомарно: выполняющийся `Extract` держит снимок, с
которым начал. Плохая строка TSV попадает в отчёт и никогда не фатальна;
источник, упавший целиком, сохраняет прежние данные.

`Gazetteer.Expand(lemma)` возвращает ключи по леммам групп вариантов
(псевдонимов с общим `Ref`), в которые входит лемма, — поле поиска может
расширить «иван» до «иоанн»; `Canonical(key)` переводит ключ обратно в
канонические формы.

**Пример:** [gazetteer](../../examples/gazetteer/main.go).

**Доступно с:** 0.2.0.

**Поведение в 0.2:**
- На вызов `Extract` фиксируется только снимок газетира: анализатор и его
  реестр словарей — живые, и после `Registry.Reload` снимок хранит
  псевдонимы, скомпилированные старым анализатором, пока хост не вызовет
  `Refresh`.
- `Refresh`, отменённый через контекст, ничего не публикует и отбрасывает
  уже пересобранные источники; следующий вызов соберёт их заново.
  Источник, у которого не удался `Version`, сохраняет свои данные, ошибка
  — в его отчёте.
- Ключи манифеста `TSVSource` — только `[a-z_]`; любая другая строка
  `# Key: value` — обычный комментарий.

## 19. Измерить качество на эталонном наборе

**Задача.** Понять, улучшило или ухудшило извлечение новое правило или
словарь, и провалить прогон CI, если какой-то тип просел.

**Как.** Храните примеры в JSON Lines: `{"id", "text", "profile", "tags",
"spans": [{"text", "type", "occurrence"}], "context"}` — эталонный спан
задаётся текстом (n-е вхождение, по умолчанию 1) и типом.
`nertest.LoadCases`/`LoadCasesFile` читают их (каждый эталонный спан
должен найтись в своём тексте); `nertest.Run(ctx, pipeline, cases)`
возвращает `Report` со строгими (точные байты и тип) и частичными
(пересечение) точностью, полнотой и F1 по типам и списком пропущенных и
лишних спанов. `Report.Write` печатает отчёт; `Report.Check(minPrecision,
minRecall)` возвращает по сообщению на каждый нарушенный порог («surname:
strict recall 0.500 < 0.900») — пустой список значит «прошло». `context` —
метаданные документа от хоста, которые lexicon игнорирует;
`nertest.WithTags(func(Case) []string)` добавляет теги, которые из них
выводит ваша функция (например, `estate:peasant` из `{"estate":
"peasant"}` — это ваше отображение, встроенного нет).

```go
rep, _ := nertest.Run(ctx, p, cases)
if v := rep.Check(0.9, 0.9); len(v) > 0 {
    t.Fatalf("golden set regressed: %q", v)
}
```

**Пример:** [golden](../../examples/golden/main.go); CLI `golden`.

**Доступно с:** 0.2.0.

## 20. Попробовать NER руками

**Задача.** Проверить, что газетир и файл правил находят во фразе, или
оценить эталонный набор, не написав ни строчки на Go.

**Как.** `lexicon extract [--gazetteer FILE]... [--rules FILE]...
[--nest OUTER>INNER]... [--tags T,...] [--types T,...] [--explain]
[--format table|jsonl] TEXT...` печатает по строке (или по строке JSON) на
спан; `-` читает документы из stdin, по одному на строку, каждый ровно в
том виде, как прочитан (смещения — от начала строки). `lexicon golden
--cases FILE.jsonl [--gazetteer FILE]... [--rules FILE]...
[--nest OUTER>INNER]... [--min-precision N] [--min-recall N]` печатает
отчёт и завершается с кодом 1 ниже порога. Морфология берётся из
`--dicts` и `--ortho`, как у `analyze`. См. [cli.md](cli.md).

**Доступно с:** 0.2.0.

**Поведение в 0.2:** CLI настраивает один профиль, `text` (все включённые
виды словарей), и для документов, и для псевдонимов, поэтому эталонный
пример с другим `profile` проваливает прогон; источник газетира называется
по базовому имени файла без расширения, поэтому `a/x.tsv` и `b/x.txt`
падают как дубликаты.

## Запланировано: паттерны

Пока недоступно; дизайн — в [спецификации](../specs/2026-09-24-lexicon-design.md)
(англ.), шаги — в [плане](../plans/2026-09-24-v0.3-patterns.md) (англ.).

- **0.3** — последовательные паттерны по спанам, леммам и граммемам с
  действиями (переразметить, усилить, выдать факт) в наборах правил
  [сценария 16](#16-слова-контекста-триггеры-и-теги-документа); спан,
  который кончается сокращением, расширяется на его точку.

Когда они выйдут, здесь появятся их сценарии со своими «Доступно с».
